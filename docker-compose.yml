services:
  db:
    image: postgres:15-alpine
    container_name: library-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: library_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d library_db || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - ./db/01_seed_manager.sql:/docker-entrypoint-initdb.d/01_seed_manager.sql      
      - db_data:/var/lib/postgresql/data
    networks:
      - library-network

  backend:
    build: .
    container_name: library-backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@db:5432/library_db"
      PYTHONUNBUFFERED: "1"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - library-network

  frontend:
    build: ./frontend
    container_name: library-frontend
    ports:
      - "3000:3000"
    environment:
      VITE_API_URL: "http://localhost:8000"
    depends_on:
      - backend
    networks:
      - library-network

volumes:
  db_data:

networks:
  library-network:
    driver: bridge
